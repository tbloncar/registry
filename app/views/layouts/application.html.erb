<!DOCTYPE html>
<html>
<head>
  <title>Simplstry</title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <link rel="stylesheet" href="http://code.jquery.com/ui/1.10.3/themes/smoothness/jquery-ui.css" />
  <%= csrf_meta_tags %>
</head>
<body>
  <header class="main-header">
    <div class="container">
        <a class="logo" href="/">Simplstry</a>
      <nav class="main-nav">
        <% if signed_in? %>
          <%= link_to "Sign Out", sign_out_url, method: :delete, class: "button sign-out-button main-header-button" %>
        <% else %>
          <%= link_to "Sign Up", "/register", class: "button sign-up-button main-header-button" %>
          <%= link_to "Sign In", sign_in_url, class: "button sign-in-button main-header-button" %>
        <% end %>
      </nav>
    </div>
  </header>
  <div class="container">
    <% if flash[:error] -%>
      <p class='error'><%=h flash[:error] %></p>
    <% end -%>
    <% if flash[:notice] -%>
      <p class='notice'><%=h flash[:notice] %></p>
    <% end -%>
    <%= yield %>
  </div>
 <%= javascript_include_tag "application" %>
</body>
</html>
